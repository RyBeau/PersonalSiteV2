stages:
  - test

test:
  stage: test
  image: node:14.15.4-alpine
  before_script:
    - apk add chromium
  script:
    - yarn install
    - yarn lint
    - yarn test-ci
  cache:
    paths:
      - node_modules
