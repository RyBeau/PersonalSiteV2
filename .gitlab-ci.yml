stages:
  - test

test:
  stage: test
  image: node:14.15.4-alpine
  before_script:
    - git config --global http.sslVerify false
  script:
    - yarn install
    - yarn lint
    - yarn test && yarn test --project a-lib
  cache:
    paths:
      - node_modules
