stages:
  - test

test:
  stage: test
  image: node:14.15.4-alpine
  before_script:
    - apt-get -qq install -y google-chrome-stable
  script:
    - yarn install
    - yarn lint
    - yarn test-ci && yarn test-ci --project a-lib
  cache:
    paths:
      - node_modules
